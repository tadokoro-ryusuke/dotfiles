# cURLデフォルト設定

# リダイレクトに従う
--location

# エラーメッセージを表示
--show-error

# レスポンスヘッダーを出力に含める
--include

# タイムアウト
--connect-timeout 30
--max-time 300

# ユーザーエージェント
--user-agent "Mozilla/5.0 (compatible; curl)"

# 失敗時にリトライ
--retry 3
--retry-delay 2

# メーターではなくプログレスバーを表示
--progress-bar

# 圧縮を使用
--compressed

# グロビングを無効化
--globoff