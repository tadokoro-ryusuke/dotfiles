# tmux GCP session configuration
# Used by alias: tmuxr

# Create windows for GCP development
new-window -n 'gcp-main'
new-window -n 'gcp-ssh'
new-window -n 'gcp-logs'

# Select first window
select-window -t 1